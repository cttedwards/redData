language: r
os: linux
warnings_are_errors: false
cache: packages
latex: false

r_packages:
    - plyr
    - usethis
    - roxygen2

apt_packages:
  - libudunits2-dev
  - libgdal-dev
  - libgeos-dev
  - libproj-dev
  - libv8-dev
  
script:
    - R CMD build .

repos:
  CRAN: https://cran.rstudio.com
